.base {
    position: relative;
    text-align: center;
    display: flow-root;
    padding: 8px;
}

.base>p:nth-of-type(odd) {
    font-size: var(--heading);
    float: left;
}

.base>p:nth-of-type(even) {
    height: 100%;
    color: blue;
}
